// 5. Check if a hardcoded number is even or odd using the modulo operator. 

fn main() { 
    let x = 63;
    if x%2 == 0 {
        println!("{} is an even number.",x);
    }else {
        println!("{} is an odd number.",x);
    }
}
